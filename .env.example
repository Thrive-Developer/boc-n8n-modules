# Repo-level environment for local release and publish automation.
# Keep real values in .env.
# GitHub Actions accepts either NPM_TOKEN or NPMJS_TOKEN as the npm fallback token.

NPMJS_TOKEN=

# Required by Keloola Accounting packages during build.
ACCOUNTING_BASE_URL=
AUTH_BASE_URL=
